namespace Core.Enum {
    /**
     * Enum for currency position.
     * Used to define whether the currency symbol should be a prefix or suffix.
     */
    export enum CurrencyPosition {
        Prefix = 'prefix', // Currency symbol before the amount
        Suffix = 'suffix', // Currency symbol after the amount
    }
}
